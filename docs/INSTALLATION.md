# Installation

Run `./install.sh`. Default application root is `~/.local/share/aag-canon-4000d`; override with `AAG_CANON_ROOT=/path ./install.sh`. Ubuntu/Debian APT dependencies are installed as needed. Uninstall with `./uninstall.sh`. System packages and downloaded images are intentionally retained.
